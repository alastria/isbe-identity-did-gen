import { Contract, JsonRpcProvider } from "ethers";
export declare class ConfigManager {
    private static instance;
    private config;
    private provider;
    private contract;
    private constructor();
    static getInstance(provider: JsonRpcProvider): ConfigManager;
    /** Recarga el contrato actual según la config */
    private reloadContract;
    getContract(): Contract;
    getModelDeployId(): string;
    getAddress(): string;
    updateConfig({ modelDeployId, didRegistryAddress, }: {
        modelDeployId?: string;
        didRegistryAddress?: string;
    }): void;
    /** Opcional: guardar cambios en el archivo físico */
    saveConfig(): void;
}
//# sourceMappingURL=ConfigManager.d.ts.map