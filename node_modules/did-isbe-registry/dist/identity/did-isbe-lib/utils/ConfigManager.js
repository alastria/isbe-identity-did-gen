/**
* Copyright (c) 2025 Comunidad de Madrid & Alastria
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
*
* You may obtain a copy of the License at
* [http://www.apache.org/licenses/LICENSE-2.0](http://www.apache.org/licenses/LICENSE-2.0 "http://www.apache.org/licenses/license-2.0")
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";
import { Contract } from "ethers";
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const CONFIG_PATH = path.resolve(__dirname, "../../../config.json");
export class ConfigManager {
    constructor(provider) {
        this.contract = null;
        this.provider = provider;
        this.config = JSON.parse(fs.readFileSync(CONFIG_PATH, "utf8"));
        this.reloadContract();
    }
    static getInstance(provider) {
        if (!ConfigManager.instance) {
            ConfigManager.instance = new ConfigManager(provider);
        }
        return ConfigManager.instance;
    }
    /** Recarga el contrato actual según la config */
    reloadContract() {
        const abiPath = this.config.didRegistry.abiPath;
        const abi = JSON.parse(fs.readFileSync(abiPath, "utf8")).abi;
        this.contract = new Contract(this.config.didRegistry.address, abi, this.provider);
    }
    /* -------------------------------------------------------------------------- */
    /*                                Getters útiles                              */
    /* -------------------------------------------------------------------------- */
    getContract() {
        if (!this.contract)
            this.reloadContract();
        return this.contract;
    }
    getModelDeployId() {
        return this.config.modelDeployId;
    }
    getAddress() {
        return this.config.didRegistry.address;
    }
    /* -------------------------------------------------------------------------- */
    /*                                Setters dinámicos                           */
    /* -------------------------------------------------------------------------- */
    updateConfig({ modelDeployId, didRegistryAddress, }) {
        if (modelDeployId) {
            this.config.modelDeployId = modelDeployId;
            console.log(`⚙️ modelDeployId actualizado a: ${modelDeployId}`);
        }
        if (didRegistryAddress) {
            this.config.didRegistry.address = didRegistryAddress;
            this.reloadContract();
            console.log(`🏗️ Contrato DID Registry actualizado a: ${didRegistryAddress}`);
        }
    }
    /** Opcional: guardar cambios en el archivo físico */
    saveConfig() {
        fs.writeFileSync(CONFIG_PATH, JSON.stringify(this.config, null, 2));
        console.log(`💾 Config guardada en ${CONFIG_PATH}`);
    }
}
//# sourceMappingURL=ConfigManager.js.map